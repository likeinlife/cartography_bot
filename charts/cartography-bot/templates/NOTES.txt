1. Ensure bot credentials are configured:
{{- if and .Values.secret.create (not .Values.secret.existingSecret) }}
   - `secret.botToken`
   - `secret.adminId`
   - `secret.databaseUrl` (required only when analytics is enabled)
{{- else if .Values.secret.existingSecret }}
   - existing secret: {{ .Values.secret.existingSecret }} with keys `BOT_TOKEN`, `ADMIN_ID`
   - optional key: `DATABASE_URL` (needed when analytics enabled)
{{- else }}
   - secret creation is disabled; provide credentials some other way.
{{- end }}

2. Check pod status:
   kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}
